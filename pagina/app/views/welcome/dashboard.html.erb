<h1>Aticulos</h1>

<% @articles.each do |article|%>
	<h2>Titulo: <%= link_to article.title, article %></h2>
	<p>Texto: <%= article.body %></p>
	<%= link_to 'Eliminar', article_path(article), method: :delete, class: "red", data: {confirm:  'Estas seguro?'}%>   <%= link_to 'Editar', edit_article_path(article) %></td>
	<%if article.may_publish? %>
	<%= link_to 'Publicar', "/articles/#{article.id}/publish" , method: :put , class: "blue" %></td>
	<% end -%>
<%end%>